{
	"name": "router",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy --env dev",
		"deploy:prod": "wrangler deploy --env prod",
		"dev": "wrangler dev --env dev",
		"dev:prod": "wrangler dev --env prod",
		"upload-folder": "node src/util/uploadFolder.js"
	},
	"devDependencies": {
		"@cloudflare/kv-asset-handler": "^0.3.0",
		"@cloudflare/workers-types": "^4.20231218.0",
		"toml": "^3.0.0",
		"typescript": "^5.0.4",
		"wrangler": "^3.0.0"
	},
	"dependencies": {
		"itty-router": "^4.0.27"
	}
}
