{
	"name": "router",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy --env prod",
		"dev": "wrangler dev --port 8787 --local-protocol https --env dev",
		"start": "wrangler dev --port 8787 --local-protocol https --env dev"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20231218.0",
		"typescript": "^5.0.4",
		"wrangler": "^3.0.0"
	},
	"dependencies": {
		"itty-router": "^4.0.27"
	}
}
